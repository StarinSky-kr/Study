#include <stdio.h>
/*
int counting(int *money, int value) {
	int count = 0;
	count = *money / value;
	*money -= count * value;
	return count;
}

int main() {
	int money = 37970;
	int* ptn = &money;
	printf("37970원은\n");
	printf("만원짜리 %d개\n", counting(ptn, 10000));
	printf("오천원짜리 %d개\n", counting(ptn, 5000));
	printf("천원짜리 %d개\n", counting(ptn, 1000));
	printf("백원짜리 %d개\n", counting(ptn, 100));
	printf("십원짜리 %d개로 구성됩니다.\n", counting(ptn, 10));
	return 0;
}
*/